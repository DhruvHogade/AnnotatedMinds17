{
  "name": "annotatedminds",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && npm run build",
    "start": "node server/index.js"
  }
}
